#include "main.h"

/**
 * mul - multiplies two number
 * @x: the first number
 * @y: the second number
 * Return: The results of the multiplication between the two numbers
 */

int mul(int x, int y)
{
	return (x * y);
}
